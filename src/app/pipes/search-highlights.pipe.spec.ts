import { SearchHighlightsPipe } from './search-highlights.pipe';

describe('SearchHighlightsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHighlightsPipe();
    expect(pipe).toBeTruthy();
  });
});
